apply plugin: "java"
apply plugin: updater.CheckMindustryUpdatesPlugin
repositories{
    maven{ url 'https://raw.githubusercontent.com/Zelaux/MindustryRepo/master/repository' }
}
supportedRepos{
    repos = [
            repo("Zelaux", "ArcLibrary"),
            repo("Zelaux", "MindustryModCore"),
            repo("Zelaux", "ArgumentParser"),
            repo("Zelaux", "AdvancedContentInfo"),
            repo("Zelaux", "JavaAsmExtension"),
    ]
}
dependencies{
    implementation "com.github.Anuken.Arc:arc-core:v145"
}
